# Aether Reef 3D

Standalone Three.js browser game.

Run locally:

```powershell
npm run serve
```

Then open `http://127.0.0.1:4197`.

Goal: collect all reef cores, keep the skiff intact, then wake the beacon.
